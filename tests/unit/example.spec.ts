import { expect } from "chai";
import "mocha";

// EXAMPLE UNIT TEST FILE

describe("This", () => {
    describe("should", () => {
        it("always pass", () => {
            expect(true).to.equal(true);
        });
    });
});